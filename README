To install ViM on MacOS:
brew install macvim

To install Vundle for ViM:

git clone https://github.com/VundleVim/Vundle.vim.git ~/.vim/bundle/Vundle.vim

run :PluginInstall to install plugins with Vundle
